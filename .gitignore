# NB : historique_ventes.json est conservé dans le dépôt à la demande du client
# (dépôt privé). Les autres exports/données restent ignorés.
ventes_tva.csv
outil_tva_galerie.json

# Artefact de distribution régénérable (zip du code déjà versionné)
outil_tva_galerie.zip

# Divers
.DS_Store
__pycache__/
*.pyc
